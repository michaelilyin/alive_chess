 оманды клиента (список далеко не полный, но это важнейшие и их нужно реализовать в первую очередь ):

»сход€щие:
  1) AuthorizeRequest(login, pass)  -- запрос авторизации пользовател€.
  2) GetSectorRequest(x,y)  -- получить нужный сектор  карты от сервера ([x,y] -- координаты сектора).
  3) UpdateWorldRequest(x,y) -- получить все объекты (строени€, короли и тп) вокруг корол€ в €чейке [x,y].
  4) GetBattleContextRequest(id)  -- получить фигуры вражеского корол€ с идентификатором id.
  5) UpdateHeroRequest(id)  -- получить фигуры и параметры своего корол€ с идентификатором id.
  6) MoveKingRequest(x,y) -- передвижение корол€ в €чейку [x,y].

¬ход€щие (идут в ответ от сервера на исход€щие):
  1) AuthorizeResponse -- подтверждение авторизации (а также получение GUID, списков своих королей и замков), либо ошибка (ErrorResponse).
  2) GetSectorResponse -- получение сектора (двумерный массив чисел) либо ошибка.
  3) UpdateWorldResponse -- получение объектов вокруг игрока (=> нужно обновить имеющиес€) или ошибка.
  4) GetBattleContextResponse -- получение вражеских фигур, либо ошибка.
  5) UpdateHeroResponse -- получить фигуры и параметры корол€, либо ошибка.
  6) ErrorResponse  -- получение описани€ и параметров возникшей ошибки.
  7) MoveKingResponse -- передвижение корол€, либо ошибка.